{
  "permissions": {
    "allow": [
      "PowerShell(node --version)",
      "PowerShell(npm --version)",
      "PowerShell(\"---\")",
      "mcp__Claude_Preview__preview_start",
      "PowerShell(try { $r = Invoke-WebRequest -Uri \"http://localhost:3000\" -UseBasicParsing -TimeoutSec 10; \"HTTP $\\($r.StatusCode\\) - server is up\" } catch { \"Not reachable: $\\($_.Exception.Message\\)\" })"
    ]
  }
}
